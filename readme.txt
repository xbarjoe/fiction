Using the code:

1. download the dataset from kaggle, the github repository mentioned in the final paper, or from box.
2. make sure the "fruits-360" folder is in the same directory as classify.py
3. run "python3 classify.py"
4. Wait about a minute for the model to load the weights.
5. when it asks for an input, give it the path to a file you want to classify.

